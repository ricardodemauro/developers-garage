// Placeholder for backend API interactions
export const createService = async (serviceType) => {
  console.log(`Create service: ${serviceType}`);
};

export const disableService = async (serviceId) => {
  console.log(`Disable service: ${serviceId}`);
};

export const getServices = async () => {
  console.log('Get services');
  return [];
};
